# AGENTS.md

MCP server that delegates coding tasks to the locally installed `pi` CLI — see [README.md](README.md)
for what it does and how it behaves.

Procedures live in [`.agents/skills/README.md`](.agents/skills/README.md): `release`, `validation`,
`pi-contract`, `testing`. Read the one covering the area you are about to touch — each carries the
incidents behind its rules, so its constraints are not guesswork.
